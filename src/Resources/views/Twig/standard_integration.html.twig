<script src="{{ src_url }}"></script>
<script>
    grecaptcha.ready(function() {
        grecaptcha.execute('{{ site_key }}', {action: 'homepage'}).then(function(token) {
            document.querySelector('input.superrb-google-recaptcha').value = token;
        });
    });
</script>
